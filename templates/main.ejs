<!DOCTYPE HTML>
<html lang="en">
<head>
    <%- include("./parts/head.ejs") %>
</head>
<body>
    <%- include("./parts/header.ejs") %>
    <%- include("./parts/settings.ejs") %> 
    <div class="wrap">  
        <div class="out1"></div>
        <div class="out2"></div>
        <div class="side">
            <%- include("./parts/info.ejs") %>

            <% if((permissEdit === 'true') && (vskillsme === 'on')) {%>
                <%- include("./parts/skills.ejs") %> 
            <% } %>
            <% if((permissEdit === 'false') && (vskillsall === 'on')) {%>
                <%- include("./parts/skills.ejs") %> 
            <% } %>

            <% if((permissEdit === 'true') && (vprojectsme === 'on')) {%>
                <%- include("./parts/projects.ejs") %> 
            <% } %>
            <% if((permissEdit === 'false') && (vprojectsall === 'on')) {%>
                <%- include("./parts/projects.ejs") %> 
            <% } %>
        </div>
        <div class="article">   
            <%- include("./parts/nav.ejs") %>  
            <%- include("./parts/article.ejs") %> 
        </div>
        <div class="blok">
            <%- include("./parts/ontopfooter.ejs") %>
        </div>       
    </div>
    <div class="footer-main-info">
        <%- include("./parts/footer.ejs") %>
    </div>
    
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="./js/service.js"></script>
    <script src="./js/view.js"></script>
    <script src="./js/controller.js"></script>
    <% if(permissEdit === 'true') {%>
        <script>
            SE.$("reg-file").addEventListener("change", () => {VW.updateAva()});
            SE.send({"userid":window.location.href}, '/showskills', VW.showSkillsList);
        </script>
    <% } %>
    <% if((permissEdit === 'false') && (vskillsall === 'on')) {%>
        <script>
            SE.send({"userid":window.location.href}, '/showskills', VW.showSkillsListForAll);
        </script>
    <% } %>

</body>
